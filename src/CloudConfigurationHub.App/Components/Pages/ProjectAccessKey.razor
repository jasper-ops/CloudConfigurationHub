@page "/projects/{ProjectId:guid}/access-key"

<PageTitle>访问密钥</PageTitle>

<ManagementWorkbench InitialView="configs" ProjectId="ProjectId" RoutePanel="access-key" />

@code {
    [Parameter]
    public Guid ProjectId { get; set; }
}
