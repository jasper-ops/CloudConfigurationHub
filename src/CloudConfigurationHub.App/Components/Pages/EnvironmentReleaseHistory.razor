@page "/projects/{ProjectId:guid}/environments/{EnvironmentId:guid}/releases"

<PageTitle>发布历史</PageTitle>

<ManagementWorkbench InitialView="configs" ProjectId="ProjectId" EnvironmentId="EnvironmentId" RoutePanel="releases" />

@code {
    [Parameter]
    public Guid ProjectId { get; set; }

    [Parameter]
    public Guid EnvironmentId { get; set; }
}
