@page "/projects/{ProjectId:guid}"

<PageTitle>项目配置</PageTitle>

<ManagementWorkbench InitialView="configs" ProjectId="ProjectId" />

@code {
    [Parameter]
    public Guid ProjectId { get; set; }
}
