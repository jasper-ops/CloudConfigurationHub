@page "/projects/{ProjectId:guid}/environments/{EnvironmentId:guid}/publish"
@rendermode InteractiveServer

<PageTitle>发布环境配置</PageTitle>

<ManagementWorkbench InitialView="configs" ProjectId="ProjectId" EnvironmentId="EnvironmentId" RoutePanel="publish" />

@code {
    [Parameter]
    public Guid ProjectId { get; set; }

    [Parameter]
    public Guid EnvironmentId { get; set; }
}
