@if (isLoading) {
    <div class="cch-empty">正在加载...</div>
}
else {
    <CascadingValue Value="this">
        @if (InitialView == "configs") {
            <ConfigurationsWorkbenchView />
        }
        else {
            <ProjectsWorkbenchView />
        }
    </CascadingValue>
}
