# Contributing

## 开发约定

- 使用 TDD：先写失败测试，再实现最小代码。
- 服务端单元测试禁止用于测试代码规范，只能将单元测试用于测试具体的功能是否满足要求
- 小步提交：每完成一个可验证的小任务提交一次。
- 提交消息使用中文，示例：`添加项目领域模型`。
- 提交前至少运行与改动相关的测试；发布前运行完整 `dotnet test`。

## 分支与发布

- 普通提交和 Pull Request 触发 CI。
- 推送 `v*.*.*` 或 `v*.*.*-*` tag 触发 SDK NuGet 发布。
